# Machine config for the sweep machine (InfiniteTalk runs only). Copy to configs/machine.env (gitignored) and edit.
# NOTE: the InfiniteTalk launchers (scripts/infinitetalk/*.sh) predate this machine.env pattern and
# do NOT source this file — they carry their own EDIT-marked path blocks at the top of each script
# instead. See docs/status-and-todo.md's re-point checklist (§ "0. Re-point paths") for the full list.
# OmniAvatar-side vars: not on this machine.
# export ODE_TRAJ_ROOT_OMNI=/home/work/.local/ode_full_trajectories  # not on this machine
# export ODE_ANALYSIS_ROOT_OMNI=/home/work/.local/ode_analysis  # not on this machine
export ODE_TRAJ_ROOT_IT=  # TODO: set on sweep machine
export ODE_ANALYSIS_ROOT_IT=  # TODO: set on sweep machine
# export WEIGHTS_ROOT=/home/work/.local/OmniAvatar/pretrained_models  # not on this machine
# export VAE_PATH=$WEIGHTS_ROOT/Wan2.1-T2V-14B/Wan2.1_VAE.pth  # not on this machine
# export TEACHER_CKPT=/home/work/output_omniavatar_v2v_phase2/step-10500.pt  # not on this machine
# export MOUTHWEIGHT_CKPT=/home/work/output_omniavatar_v2v_maskall_refseq_mouth_weight_4gpu/step-6000.pt  # not on this machine
# export RECON_DATA_DIR=/home/work/stableavatar_data/v2v_validation_data/recon  # not on this machine
# export NEG_TEXT_EMB=/home/work/stableavatar_data/neg_text_emb.pt  # not on this machine
export METRICS_ROOT=/data/karlo-research_715/workspace/kinemaar/paul/eval_metrics
export INFINITETALK_ROOT=/data/karlo-research_715/personal/hyunbin/reference_FastGen_InfiniteTalk/InfiniteTalk
# export PY_OMNI=/home/work/.local/miniconda3/envs/omniavatar/bin/python  # not on this machine
# export PY_FASTGEN=/home/work/.local/miniconda3/envs/fastgen/bin/python  # not on this machine
export PY_IT=/data/karlo-research_715/workspace/kinemadae/projects/paul/videos/lip/.venvs/infinitetalk-ode/bin/python
